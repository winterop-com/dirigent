# One target: the collector's Prometheus exporter. Dirigent itself is never scraped.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: dirigent
    static_configs:
      - targets: ["otel-collector:8889"]
